import{createPinia}from'pinia';import{mount}from'@vue/test-utils';import ElementPlus from'element-plus';import{describe,expect,it}from'vitest';import RenameView from'./RenameView.vue';describe('重命名工作台',()=>{it('显示四步流程及匹配统计',()=>{const w=mount(RenameView,{global:{plugins:[createPinia(),ElementPlus]}});expect(w.text()).toContain('1　选择目录');expect(w.text()).toContain('4　确认执行');expect(w.text()).toContain('匹配：')})})
