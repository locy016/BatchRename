import{test,expect}from'@playwright/test';test('窄屏保持结果区域可见',async({page})=>{await page.setViewportSize({width:760,height:680});await page.goto('/#/rename');await expect(page.getByText('当前目录')).toBeVisible()});test('深色外观完整应用到设置抽屉',async({page})=>{await page.goto('/#/rename');await page.locator('.theme-select').click();await page.getByText('深色',{exact:true}).click();await page.getByRole('button',{name:/设置/}).click();const drawer=page.getByRole('dialog',{name:'扫描与命名设置'});await expect(drawer).toBeVisible();await expect(page.locator('html')).toHaveAttribute('data-theme','dark');await expect(drawer).toHaveCSS('background-color','rgb(23, 30, 43)');await expect(drawer.locator('.setting-card').first()).toHaveCSS('background-color','rgb(23, 30, 43)')})
