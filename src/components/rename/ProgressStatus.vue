<script setup lang="ts">import{useRenameStore}from'../../stores/rename';const s=useRenameStore()</script><template><div v-if="s.busy" class="progress"><el-progress :percentage="s.busy==='scanning'?50:100" :indeterminate="s.busy==='scanning'"/><span>{{s.busy==='scanning'?'正在扫描目录…':s.busy==='previewing'?'正在生成预览…':'正在安全执行…'}}</span></div></template><style scoped>.progress{display:grid;grid-template-columns:180px 1fr;gap:12px;align-items:center;color:var(--muted);font-size:13px}</style>
