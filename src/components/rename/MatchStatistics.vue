<script setup lang="ts">import{useRenameStore}from'../../stores/rename';const s=useRenameStore()</script><template><footer class="stats"><span>匹配：<b>{{s.summary.matched}}</b> 项</span><span>可修改：<b class="ok">{{s.summary.ready}}</b></span><span>无变化：{{s.summary.unchanged}}</span><span>冲突：{{s.summary.conflicts}}</span><span>阻止：{{s.summary.invalid}}</span></footer></template><style scoped>.stats{display:flex;gap:22px;min-height:46px;align-items:center;padding:0 16px;color:var(--muted);border-top:1px solid var(--border)}b{color:var(--text)}.ok{color:var(--success)}</style>
