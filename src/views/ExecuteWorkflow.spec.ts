import{createPinia,setActivePinia}from'pinia';import{describe,expect,it}from'vitest';import{useRenameStore}from'../stores/rename';describe('执行门禁',()=>{it('只有有效预览存在可修改项目时允许执行',()=>{setActivePinia(createPinia());const s=useRenameStore();s.scanJobId='job';s.previewValid=true;s.summary.ready=0;expect(s.canExecute).toBe(false);s.summary.ready=2;expect(s.canExecute).toBe(true)})})
