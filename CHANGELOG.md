# 更新日志

## 2.0.0-alpha.1 — 2026-09-03

本轮迁移验收以旧版 194 项回归作为行为基线，逐项核对目录浏览、匹配、预览、安全执行、日志、撤回、主题与响应式窗口。文件名管理 2.0 现已达到完整功能对齐：结果显示数量可在 1–100 条间调整且不会改变完整统计，操作日志覆盖全部生命周期状态并呈现成功、跳过、失败和撤回数量，整批撤回具备独立风险确认、不可关闭的逐项进度窗口以及完整或部分完成反馈。

启动窗口会依据当前显示器工作区自适应：标准比例采用约一半宽高，超宽屏与竖屏使用受约束的内容比例，同时保证 1180×760 的产品布局下限。扫描、预览、执行与撤回的耗时处理均与界面事件循环隔离，大目录和批量磁盘操作期间仍可持续刷新进度。

迁移验收通过后，Python 子项目退出维护并从主仓库清理。稳定规则夹具、`schema_version = 1` 日志样本和既有性能报告继续保留，确保历史数据兼容性与迁移结论仍可复核。

## 2.0.0-alpha.1 — 2026-09-02

BatchRename 2.0 将桌面核心迁移到 Rust，并以 Tauri 2、Vue 3 和 Element Plus 重建交互层。扫描、预览、执行、操作日志与撤回形成清晰边界，大目录工作不再让界面承担递归遍历或完整结果集。

本版本保持 Python 版 `schema_version = 1` 操作档案、中文状态、扩展名保护和 Python 捕获引用语义。十五个经典正则模板通过共享夹具同时验证；历史记录仍位于原有本地目录，稳定日志样本可由新内核完整读取。

工作台采用紧凑的四步左侧流程和高密度结果区，名称未变化仍计入匹配并解释原因。结果表使用相对目录、紧凑类型/状态图标和强调色新名称；撤回安全检查与执行统一收纳在操作日志详情中，三态主题、帮助与关于形成完整产品界面。

同机开发构建基准中，1,000 项扫描从 Python 的 15.028 ms 降为 Rust 的 5.108 ms，10,000 项从 71.869 ms 降为 33.876 ms；预览分别为 13.331/11.913 ms 与 152.991/118.815 ms。性能改进没有绕过冲突检查、逐项原子写档或撤回预检。

发布验证包含 12 项前端测试、27 项 Rust 集成断言、4 项 Playwright 场景、194 项 Python 回归和跨语言日志往返。已生成 Windows NSIS 安装候选与便携 EXE，并完成真实窗口启动及进程清理冒烟测试。
