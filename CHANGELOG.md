# 开发日志

本日志记录“批量重命名”从需求到可发布程序的设计过程、问题根因、技术取舍和验证结论。README 面向使用者介绍当前产品；开发过程统一保留在这里。

## 2026-09-01：多分辨率窗口与响应式工作台

### 从固定窗口到显示器感知

此前主窗口始终以 960×680 启动，在1080P上便于并排使用，但在2K和4K标准屏上只占很小区域，结果表无法直接利用可用空间；若简单按屏幕百分比放大，21:9和32:9显示器又会产生过宽表格，竖屏则会让固定侧栏挤压结果区。本轮把“屏幕初始尺寸”和“窗口内部布局”拆成两个独立判断：前者只看鼠标所在显示器的工作区及宽高比，后者只看窗口当前客户区宽度。

工作区宽高比小于1.15视为竖屏，1.15至2.0视为标准屏，大于2.0视为超宽屏。标准屏宽高各取工作区50%；超宽屏高度取68%、宽度取46%并把内容比例限制在1.7以内；竖屏宽度取90%、高度取68%并限制在宽度的1.15倍以内。所有结果保留960×680下限，在工作区允许时不越界并居中。Windows通过鼠标位置与显示器工作区接口选择初始屏幕，接口失败时回退Tk虚拟桌面；负坐标副屏使用Tk要求的绝对负坐标形式，避免被误解释为相对主屏右边缘的位置。

### 三档工作台与原地重排

客户区宽度960至1119进入紧凑模式，1120至1439进入标准模式，1440及以上进入舒展模式。根窗口缩放事件使用120毫秒防抖，同一档位内只有尺寸发生有效变化时才调整；目录输入框、查找与替换变量、匹配快照、预览候选和执行状态始终保留原实例，不通过销毁重建实现响应式效果。

发布前独立审查进一步覆盖1440×2560与2160×3840高分辨率竖屏，发现只按宽度断点会让这两类窗口分别误入标准或舒展模式。响应式判定因此同时读取客户区宽高比：比例小于1.15时始终进入紧凑工作台，其余窗口再按宽度断点分档。这样竖屏启动后的首个Configure事件不会推翻初始屏幕分类，手动把窗口调整为高而窄时也会使用抽屉保护结果区宽度。

结果表为三档布局分别设置六列初始宽度。类型、原名称、新名称和状态在最窄模式仍保持可见，所在目录与说明优先压缩并继续由悬浮提示展示全文；舒展模式把更多空间分配给路径、名称和说明。打开的模板或设置面板会在重排后重新计算锚点和边界，不会停留在旧侧栏坐标。

### ChatGPT App式紧凑导航

紧凑模式把完整工作流折叠为64像素深蓝导航条，顶部入口展开约300像素宽的窗口内抽屉，底部保留正则模板与设置入口。抽屉直接复用标准模式中的目录、规则、扫描、替换、预览和执行控件，因此切换窗口尺寸不会产生两套重复内容或丢失状态。工作流抽屉、正则模板和设置三者互斥；重复点击、Esc或点击结果区会收起当前浮层。任务忙碌时仍允许关闭抽屉，但不允许重新打开或改变规则。

真实Windows窗口分别检查了标准工作台、960×680紧凑导航和展开抽屉。200% Tk缩放回归同时确认工作流抽屉、正则模板和设置面板的右侧与底部均位于主内容区内。第一次标准截图发现52像素类型列在当前DPI下把“文件夹”截断，字体测量证明三字标签与内边距实际需要70像素；三档类型列因此统一为72像素，并加入高DPI完整标签回归测试。

README标准截图为1284×797，紧凑抽屉截图为964×757，均来自真实Tk窗口。演示状态使用“项目 → 客户”的7项安全预览，只向界面注入候选数据，没有扫描实际目录，也没有调用磁盘重命名执行器。紧凑截图先把同一窗口从标准档缩放到960×680，再展开复用的工作流抽屉，同时验证缩放后输入和预览仍在。

### 审查、验证与单文件构建

独立代码审查覆盖窗口几何、负坐标副屏、极小工作区、首个Configure事件、防抖生命周期、状态保留、浮层互斥、忙碌态和高DPI边界。审查发现的高分辨率竖屏分档问题已修复，并补齐防抖只应用最后一次尺寸、窗口销毁后清理挂起回调的自动化测试；复审未发现新的Critical或Important问题。

本次使用Python 3.13.12、pytest 8.3.2、PyInstaller 6.19.0和Windows 11完成发布验证：

- `batch_rename`、`tests`与`main.py`通过字节码编译，Git差异检查无尾随空白或冲突标记。
- 源码验证和构建脚本内部验证均收集111项测试并全部通过，覆盖1080P、2K、4K、21:9、32:9、三档布局、两种高分辨率竖屏以及100%、150%、200% Tk缩放。
- PyInstaller继续使用无控制台、单文件和项目ICO配置生成`dist\BatchRename.exe`。
- 冒烟验证只跟踪本次启动产生的PID 41992与25028；PID 25028在12秒门限内创建标题为“批量重命名工具”的可见窗口，验证后仅终止这两个进程，剩余0个。
- 经验证产物已同步到主项目`D:\ProjectFolder\BatchRename\dist\BatchRename.exe`，复制前后文件大小和SHA-256一致。
- 最终文件大小为12.30 MB（12,893,273字节）。
- SHA-256：`187227863740BC60402D6080E29116867D3D040665DA7ADA73A5678171891AA8`。

## 2026-09-01：README 产品说明与六步操作视觉

### 从功能目录转向使用路径

README 原有内容已经覆盖多数能力，但界面介绍、紧凑布局、正则示例和安全策略分散在多个长章节中，第一次访问项目的读者需要先理解实现细节，才能拼出完整操作顺序。本轮改为按照真实使用路径组织：先说明产品定位和预发行状态，再通过主界面、六步流程、两阶段职责、正则模板、安全策略和开发路线逐步展开。

扫描、结果预览和确认执行使用独立章节与对照表说明。扫描只生成名称匹配快照，结果预览从快照计算新名称和状态，确认执行才写入磁盘；替换内容相同的项目仍属于匹配结果。README 不再把查找框回车、扫描和结果预览写成同义入口，也不把撤回管理或操作日志描述为已经实现。

### 仓库内六步流程图

操作示意图使用 1440×360 的仓库内 SVG，而不是依赖外部绘图服务或运行时脚本。六张卡片依次表示选择目录、查找规则、扫描匹配、填写替换、结果预览和确认执行，颜色沿用应用的深蓝、青绿色和浅灰视觉体系。扫描与预览采用强调色，最终执行使用深蓝卡片；底部两条安全说明明确前五步不会写入磁盘，只有“可修改”项目会在二次确认后执行。

SVG 提供标题、描述、语义角色和中文字体回退，在 GitHub 中可直接渲染。文档测试会解析 XML、核对 `viewBox`，并要求 README 同时引用主界面、正则模板和流程图三项产品视觉，避免后续改名或移动资源造成破图。

### 真实界面预览

主截图继续来自 DPI 感知的真实 960×680 Tk 窗口。截图程序在系统临时目录中创建中文演示文件夹和文件，以“项目 → 客户”完成扫描和安全预览，共匹配 7 项且 7 项可修改；只设置界面预览状态，没有调用执行器。截图完整显示顶部菜单、左侧流程、四项统计、自然排序结果、新名称强调、进度与底部工具入口。

新增正则模板截图来自另一实例的真实主窗口，只打开默认“日期时间”分类和“压缩标准日期”模板。分类、列表、只读表达式、替换内容、处理示例与“一键应用此规则”按钮均在窗口边界内；没有应用模板、扫描目录或修改磁盘。两张 PNG 均为 982×766，包含 Windows 窗口边框后的物理捕获结果。

### 内容边界与验证

README 使用当前界面中的真实名称：`扫描`、`结果预览`、`确认执行`、`正则模板`、`设置`和`功能 → 结果详情`。开发日志保留截图生成方式、设计取舍和验证口径，README 只介绍使用者需要理解的产品能力与安全边界。

新增 `tests/test_documentation.py` 自动提取 README 的本地 Markdown 图片引用，逐项检查文件存在，并解析流程 SVG。这样文档资产和源码测试一起进入构建前验证，不依赖发布时人工发现缺图。

最终验证收集 76 项测试并全部通过，其中 3 项专门覆盖 README 本地视觉引用、图片存在性和 SVG XML；`batch_rename`、`tests` 与 `main.py` 通过字节码编译，旧界面描述检索无结果，Git 差异检查与合并后的主工作树状态正常。功能分支以 `--ff-only` 快进进入本地 `main`，没有改写或压缩此前的中文开发提交。

## 2026-09-01：1.1.0-beta.1 两阶段工作台与正式导航

### 为什么拆分扫描和预览

旧界面把查找、替换和预览压缩成一次动作，使用者仅想查询名称时仍会被要求理解替换规则，替换内容变化也会重新读取完整目录。核心现在先生成只包含路径、类型和查找规则的匹配快照，再从快照计算目标名称与安全状态。扫描不依赖替换内容；预览阶段不再调用目录遍历。旧的组合扫描接口保留为两步操作的兼容封装，因此核心调用方和执行安全策略没有分叉。

界面状态也随之明确为“等待扫描、已有匹配、已有预览、正在执行”。目录、层级、对象类型、查找内容和正则模式会使匹配快照失效；替换内容及扩展名设置只使预览失效；显示上限只重绘表格。扫描后的行保留原名称并提示等待预览，预览后才显示青绿色新名称和可修改、名称未变化或阻止执行状态。

### 左侧流程与不重复的顶部菜单

原来的范围卡片、规则卡片和横向操作条在 960×680 下形成多处视觉起点，也让“扫描”和“结果预览”的职责显得相同。新工作台将选择目录、规则模式、查找内容、扫描、替换内容、结果预览和确认执行固定为左侧纵向顺序；右侧只保留统计、统一结果表和进度。结果表初始列宽重新压缩，长路径和说明继续通过悬浮提示完整显示，窗口放大后额外空间由结果区吸收。

顶部原生菜单只保留文件、功能和帮助三类全局入口。结果详情随执行记录启用；撤回管理和操作日志以禁用的“开发中”项目占位，为紧接着的 1.1.0 开发提供稳定入口，但本次没有伪造尚不存在的功能。使用说明和关于不再以主界面按钮重复出现。

### 浮动工具和多显示器窗口

正则模板与设置收拢到左下角入口，通过主窗口内的浮动卡片显示。两个面板互斥，重复点击、Esc 或点击结果区会关闭当前面板。模板仍按六类十五条真实规则组织，并在卡片内展示表达式、替换内容和处理前后示例；设置卡集中层级、文件夹/文件范围与扩展名保护，同时解释每项设置会使扫描还是预览失效。

使用说明、关于和执行结果详情采用统一子窗口管理。定位算法以父窗口为中心并限制到父窗口所在显示器的工作区；Windows 使用显示器工作区 API，异常或其他平台回退到 Tk 虚拟桌面。相同窗口只保留一个实例，重复打开会提升并恢复焦点，关闭后注销并把焦点交还主窗口，解决多屏环境下弹窗散落和没有焦点的问题。

### 预发行信息

版本更新为 `1.1.0-beta.1`。关于页面以产品能力、开发路线、安全提示和联系方式组织内容，明确当前已实现两阶段扫描预览、统一结果、安全拦截、正则模板、多层目录及处理进度；撤回管理和操作日志仍在开发中。页面同时说明软件处于快速开发期，重要目录需要备份，规则、预览和最终执行由使用者自行确认。作者邮箱 `lo.c@live.cn` 以可选择的只读文本显示，不主动打开外部邮件程序。

### 验证与单文件构建

本次预发行在 Python 3.13.12、pytest 8.3.2、PyInstaller 6.19.0 和 Windows 11 环境完成验证：

- 源码检查和最终构建脚本内部检查分别执行完整测试，均收集 73 项并全部通过；`batch_rename`、`tests` 和 `main.py` 同时通过字节码编译，Git 差异不存在尾随空白或冲突标记。新增用例覆盖审查发现的浮层边界、模板忙碌态锁定、结果区点击收起和扫描自然排序。
- 200% Tk 缩放回归测试确认 960×680 下正则模板、设置两个底部入口仍在左侧可视边界内，四项匹配统计的实际宽度不小于文字请求宽度。该测试来自真实截图中发现的侧栏溢出和统计截断问题。
- README 截图以源码窗口真实生成：在项目工作树中扫描名称含 `test` 的项目，再以“示例”生成安全预览，共匹配 20 项且 20 项可修改。截图只构造预览状态，没有执行任何重命名。
- PyInstaller 使用无控制台、单文件和项目 ICO 配置生成 `dist\BatchRename.exe`。冒烟验证只启动本次新产物，在 12 秒限制内发现标题为“批量重命名工具”的可见窗口，随后只停止这次启动产生的两个进程。
- 经验证的产物已同步到主项目 `D:\ProjectFolder\BatchRename\dist\BatchRename.exe`，复制前后 SHA-256 一致。
- 最终文件大小为 12.29 MB（12,889,728 字节）。
- SHA-256：`11C2E0D0B3DC506F61F257CC95BDE6F10438D170A762BBADE59E3AAF074F2B6A`。

## 2026-08-27：规则预览路径与结果确认布局

### 设置区比例与预览容量

上一版已把默认窗口收进 960×680，但扫描范围和重命名规则仍然等宽。目录与层级只需要较短字段，查找、快捷动作、替换内容和对象选项却共享另一半空间，导致真正频繁编辑的区域反而更局促。本轮使用 35 与 65 的网格权重分配两张卡片；窗口加宽时仍按同一比例扩展，默认尺寸下查找与替换保持在一行。

第一次实现只写入权重，没有加入比例分组。真实窗口截图显示 Tk 会先满足目录输入框和选择按钮的请求宽度，最终卡片接近 45/55；配置值正确并不代表渲染比例正确。回归测试随后改为在可见的 960×680 窗口中读取两张卡片的实际宽度。两列加入同名分组后，Tk 按 35:65 权重分配最终空间；目录输入框的初始字符宽度同时收紧，但仍会随卡片拉伸，最终同时满足比例与 960 像素请求宽度。

预览上限从 10 调整为 100。该数值只决定表中展示多少条，不参与匹配计数，也不会缩小确认执行的候选范围。匹配统计从窗口创建时就完整显示“匹配、可修改、名称未变化、阻止执行”四种口径，扫描完成只替换数字，不再因为初始空白或省略字段造成统计含义变化。高 DPI 实际截图曾显示最后一段被右侧预览上限挤掉，因此统计文本改用紧凑间距，并增加“控件实际宽度不得小于文字请求宽度”的可见窗口测试。

### 从规则输入直接查看结果

查找框右侧增加“扫描”，并支持在查找框按 Enter。它们与操作区更名后的“结果预览”都绑定同一个扫描方法，因此目录、层级、文件夹与文件范围、正则模式、扩展名保护和替换内容始终使用同一套校验。扫描期间三个入口随其他设置一起锁定，避免后台读取时重复发起任务。

自动化交互测试最初在隐藏的 Tk 测试窗口中生成回车事件，按钮调用成功而键盘调用没有发生。绑定脚本检查和最小窗口实验表明生产绑定存在；只有窗口从隐藏状态切换为可见并让查找框获得焦点后，Tk 才会派发合成的键盘事件。测试因此改为模拟真实可见窗口，而没有为测试环境改写应用事件流程。

### 结果列顺序与新名称强调

统一结果表按“类型、所在目录、原名称、新名称、状态、说明”排列，先交代对象位置，再展示名称对照。列标题与候选值写入顺序由同一组回归测试锁定，防止只移动表头而数据仍落入旧列。

ttk Treeview 的标签颜色只能作用于整行，直接给新名称设置颜色会覆盖可修改、名称未变化和阻止执行的状态色。界面因此在新名称列上维护轻量文本覆盖层：只复用当前可见行所需的标签，使用青绿色文字，并根据单元格边界跟随窗口缩放、纵横滚动、选择和数据刷新。点击覆盖文字仍会选中原记录，悬停可查看完整新名称；离开可视区域或清空结果后标签会被回收隐藏。

### 文字与数值组件

字段标题、单位文字和匹配统计分别使用独立的字体、颜色与间距。层级和预览上限数值框统一居中显示，焦点边框、活动箭头与禁用状态沿用同一套深蓝和青绿色视觉语言。验证中还确认 clam 主题的 Spinbox 选区颜色不是可由 ttk 样式元素可靠覆盖的属性，因此没有保留一项只在样式配置字典中存在、实际控件不使用的伪装饰，而以真实可见的边框、箭头和排版作为状态反馈。

### 验证与发布记录

本轮使用 Python 3.13.12、pytest 8.3.2、PyInstaller 6.19.0 和 Windows 11 完成发布验证：

- 完整测试收集 56 项，发布前测试与构建脚本内部测试均全部通过；`batch_rename`、`tests` 与 `main.py` 同时通过字节码编译检查。
- 在独立临时目录中使用“项目 → 项目”查询两个真实名称，得到匹配 2 项、可修改 0 项、名称未变化 2 项、阻止执行 0 项，确认查询语义没有退回到零结果。
- README 主界面截图来自 960×680 源码窗口的真实扫描：共匹配 6 项，其中可修改 5 项、目标冲突 1 项。截图文件为 964×727 RGB PNG，显示 35/65 设置区、完整统计、100 条预览上限、结果列顺序和青绿色新名称。
- PyInstaller 以无控制台单文件模式完成构建，生成 `dist\BatchRename.exe`；启动后在 12 秒门限内创建标题为“批量重命名工具”的可见窗口，随后只结束本次冒烟验证新增的两个进程。
- 已验证构建同步到主项目 `D:\ProjectFolder\BatchRename\dist\BatchRename.exe`，复制前后 SHA-256 完全一致。
- 最终文件大小为 12.28 MB（12,874,698 字节）。
- SHA-256：`DEC9AAC60635BB09D2FDD4258D4E56B887844DDD0F9F47D4D66E534633C90FCA`。

## 2026-08-27：紧凑自适应界面与正则模板库

### 尺寸目标与空间诊断

这一轮以 960×680 作为完整可操作的默认与最小窗体。旧界面虽然已经形成卡片层级，但 1240×820 的固定尺寸不利于和资源管理器并排使用；查找与替换上下排列，结果表只显示五行，横向滚动条即使不需要也始终占据一行。

第一版尺寸调整只改了窗口几何，自动化诊断随即发现控件请求高度仍为 801。强制显示在 680 像素高的窗口会造成底部区域裁切，因此没有把几何数字当作适配完成。逐区测量显示，标题图标、卡片说明、按钮内边距、30 像素结果行和永久横向滚动条共同造成超额。

最终布局缩短标题区，收紧卡片与操作区间距，把查找和替换放在同一行，并将结果行调整为适合密集预览的高度。可从卡片获取的层级说明保留在悬浮提示中，不再重复占据固定行。样式完整应用后，主界面请求尺寸为 827×667，小于 960×680；窗口放大时，唯一获得额外纵向空间的是结果列表。

### 长内容与滚动策略

结果表继续保留类型、原名称、新名称、所在目录、状态和说明六列。横向滚动条改为读取表格可见比例：全部内容可容纳时释放网格空间，出现水平溢出时恢复到原位置，不改变当前滚动进度。

鼠标移动到结果单元格时，程序识别所在行与列，并使用当前 Treeview 字体测量完整文字宽度。只有文字实际超过可见列宽时才显示悬浮信息卡；卡片同时给出字段名称与完整文本。点击、移出或滚动会取消等待并关闭卡片，弹出位置也会限制在当前屏幕范围内。

### 桌面控件视觉系统

界面继续使用 Tkinter/ttk，没有引入新的运行依赖。输入框、数值框、下拉选择、复选框、单选框、主次按钮、横纵滚动条、进度条和状态栏分别使用专用样式，统一浅色字段、品牌色焦点、悬停反馈与禁用颜色。普通控件说明和结果全文提示改为相同的浅色信息卡，避免系统默认黄色提示与主界面割裂。

### 从示例到模板库

原来的四条示例能解释捕获组，却不足以覆盖日常整理，也要求使用者先理解表达式。新的模板库按日期时间、编号整理、标签清理、文本清理、片段调整和扩展名六类组织十五条规则。日期连接、日期分隔统一、日期移位、图片序号、中文编号、括号标签、首尾空白、连续空格、混用连接符、固定前后缀、片段交换和 JPEG 扩展名整理都提供真实处理前后名称。

模板选择器先展示用途和效果，再显示只读表达式与替换内容。选择分类只过滤可见项目，不修改全局模板数据；双击或“一键应用此规则”会填写查找、替换、正则模式及扩展名选项，但不会启动扫描。涉及扩展名的模板会显示单独提醒并自动开启完整文件名处理。

每条模板都通过真实 `RenameRule` 计算结果，不依赖手写结果字符串自证。任一表达式、捕获组或扩展名选项与说明不一致，测试会直接失败。

### 测试环境稳定性

扩充界面测试后，Windows 上连续创建多个独立 `Tk` 解释器会偶发报告 Tcl 资源文件暂时无法读取。失败位置在不同用例间移动，单独运行均通过，连续五轮统计中有三轮出现该环境错误，表明它并非业务断言失败。

界面测试随后改为每次 pytest 进程只初始化一次 Tcl/Tk，具体用例使用独立 `Toplevel` 隔离窗口状态。调整后同一界面测试连续五轮全部通过，既保留控件级真实测试，又避免重复加载系统 Tcl 资源造成的随机噪声。

### 验证与发布记录

发布收尾继续使用 Python 3.13.12、pytest 8.3.2、PyInstaller 6.19.0 和 Windows 11：

- 完整测试收集 52 项，源码验证和构建脚本内部验证均全部通过。
- `batch_rename`、`tests` 与 `main.py` 通过 Python 字节码编译检查。
- 960×680 源码窗口实际启动后，顶部设置、十行结果、进度和状态区域均完整可见；最大化后额外高度全部进入结果列表。
- 使用当前功能工作树执行普通文本 `test → test` 查询，实际得到匹配 20 项、可修改 0 项、名称未变化 20 项，确认相同替换内容不会被误报为零结果。
- 六类模板选择、只读表达式、处理前后对照、一键应用按钮、输入焦点样式和悬浮提示完成实际窗口检查。
- PyInstaller 以无控制台单文件模式成功生成 `dist\BatchRename.exe`，冒烟启动后获得标题为“批量重命名工具”的可见窗口。
- 最终文件大小为 12.27 MB（12,868,635 字节）。
- SHA-256：`302796F632F86B2FE45C30AFC58751646585B084E38A7161CD681505245FD54A`。

## 2026-08-27：搜索语义与界面体验改版

### 背景

实际使用时，以“众川”为查找内容，同时把替换内容也设为“众川”。目录中存在名称符合条件的项目，但扫描结果显示为 0。这个表现容易让人误以为程序没有读到文件，也使工具无法单独承担名称查询和规则测试的用途。

同一版本的界面还存在三个体验问题：文件夹和文件分别放在两个标签页里，不便于整体查看；正则提示只有零散片段，没有完整可运行的输入和结果；原生控件缺乏视觉层级，在高 DPI 环境中也需要更可靠的尺寸处理。

### 根因定位

扫描核心原先先计算替换结果，再执行以下判断：当新名称与原名称相同时直接跳过候选项。这意味着“名称符合搜索条件”与“名称将被修改”被当成了同一件事。

最小复现使用临时目录创建 `众川合同.docx`，以“众川 → 众川”执行扫描。旧逻辑返回 0 个候选项，回归测试稳定复现了问题。仅在受保护扩展名中命中搜索文本时也有同样表现，例如默认保护扩展名时查找 `jpg`，`照片.jpg` 不会进入结果。

### 语义调整

扫描现在分成两个判断阶段：

1. 使用完整名称判断是否符合搜索条件。
2. 对允许修改的名称部分计算替换结果和安全状态。

只要名称匹配，项目就进入结果列表。替换后名称相同的项目使用“名称未变化”状态；如果只命中受保护扩展名，则进一步说明扩展名保护是没有执行动作的原因。执行器仍只处理“可修改”状态，因此查询能力的增强没有放宽任何磁盘写入条件。

统计口径也随之拆分为匹配总数、可修改数、名称未变化数和阻止执行数。匹配存在但没有可执行项目时，状态区会明确提示“已匹配 N 项，但本次没有可执行动作”。

### 结果列表与正则示例

文件夹和文件合并到同一张结果表，通过“类型”列区分。排序先按文件夹和文件分类，再使用名称自然顺序，最后用所在目录保证同名项目顺序稳定。预览上限只截取界面显示结果，不改变完整统计和执行范围。

正则示例不再以提示语片段存在，而是独立为经过实际规则运行验证的数据。日期压缩、图片序号、删除标签和片段交换四组示例都包含用途、查找表达式、替换内容、原名称和结果。示例窗口可以直接把选中规则填入主界面，并自动启用正则模式。

### 界面设计与高 DPI 调试

界面继续采用标准库中的 Tkinter/ttk，避免为视觉改版增加运行依赖和扩大单文件体积。新的视觉体系使用深蓝标题区、青绿色主操作按钮、浅灰背景、白色设置卡片和按状态区分的结果颜色。扫描范围与重命名规则并列显示，操作与统计集中在同一条工具栏，统一结果表占据窗口主要空间。

视觉验证过程中，早期截图出现右侧被裁切的现象。控件请求尺寸诊断表明页面实际宽度小于窗口宽度，最终确认截图程序读取了 Windows DPI 虚拟化坐标，导致截图范围偏移，并非控件真实溢出。与此同时，原布局的请求高度确实超过默认窗口，因此增加了回归测试：默认布局请求高度必须不大于 820。结果表使用较小的初始可见行数，并在窗口扩大时自动填充空间。

程序运行入口在创建 Tk 之前启用 Windows 系统 DPI 感知。随后使用 `pythonw` 启动并以物理窗口边界重新截图，确认帮助、目录选择、正则示例、预览上限、统一列表和底部进度区完整可见。

### 应用图标

应用图标使用图像生成工具制作，核心图形由深蓝文件夹、两张文档和青绿色循环箭头组成，不包含文字。图形保留透明背景和宽轮廓，兼顾窗口标题栏与任务栏小尺寸辨识度。

项目保存 1254×1254 RGBA PNG，并转换为包含 16、24、32、48、64、128 和 256 像素资源的 ICO。PNG 用于 Tk 窗口和顶部品牌区，ICO 嵌入 PyInstaller 产物；两种资源都随单文件运行时数据一起收集。

### 当前验证记录

- “众川 → 众川”会返回真实匹配项，并标记名称未变化。
- 只命中受保护扩展名时会列出匹配项并解释原因。
- 正则示例全部使用真实规则验证输入和输出。
- 单列表分类、自然排序、统计和忙碌状态锁定均有自动化测试。
- PNG 透明通道、ICO 多尺寸、PyInstaller 图标引用和窗口图标加载均有测试。
- 现代化窗口已在 Windows 高 DPI 环境实际启动并完成截图检查。

### 发布收尾

发布验证在 Python 3.13.12、pytest 8.3.2、PyInstaller 6.19.0 和 Windows 11 环境完成：

- 完整测试收集 46 项，全部通过；构建脚本内部再次运行相同测试，结果仍为 46 项通过。
- `batch_rename`、`main.py` 和测试代码全部通过 Python 字节码编译检查。
- PyInstaller 成功收集 Tk、PNG 和 ICO 资源，并使用无控制台单文件模式生成 EXE。
- 启动 `dist\BatchRename.exe` 后成功创建标题为“批量重命名工具”的窗口，随后只终止本次冒烟验证启动的进程。
- 最终文件大小为 12.26 MB（12,857,113 字节）。
- SHA-256：`23FB8296723CA87A0EB3DA7E5B8CC83F52D8676D3B9F54DE5E1B43253142FFE6`。

## 2026-08-27：首个可运行版本

### 产品范围

首个版本确立了安全批量重命名的核心流程：选择根目录、设置 1–N 或全部层级、输入普通文本或正则规则、分类预览、二次确认、逐项进度和结果详情。运行核心与 Tkinter 界面分离，便于使用临时目录对真实文件系统行为进行测试。

### 安全边界

根目录本身不参与改名；符号链接不跟随；目标已存在或多个来源生成同一目标时不覆盖；非法 Windows 名称提前阻止；执行前后二次检查文件系统状态。项目按照路径深度从深到浅执行，避免父文件夹提前改名导致子项目路径失效。

### 发布方式

PyInstaller 配置以无控制台单文件方式构建 Windows EXE。构建脚本在打包前运行完整测试，并兼容 Windows PowerShell 5 对脚本编码的处理方式。
